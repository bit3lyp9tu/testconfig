config:
    script_execution:
    variable_infix_char: "%"
    variables:
unit-test:
    syntax_scheme:
hooks:
    general_setup:
      attributes:
        path: "a_random_path"
      commands:
        - "echo the stored path is $path"
    general_shutdown:
    file_setup:
    file_shutdown:
    function_setup:
    function_shutdown:
